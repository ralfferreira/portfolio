import React from 'react'
import { Container, Button, Text } from './styles';

const Curriculum = () => {
  return (
    <Container>
      <Button>
        <Text>Currículo</Text>
      </Button>
    </Container>
  );
};

export default Curriculum;
